# PRD：OPS 权限管家（Android）

> 版本：v0.1（草稿，待用户确认）
> 日期：2026-08-17
> 状态：待确认

---

## 一、问题陈述

Android 系统自带的权限管理界面（设置 → 应用 → 权限）只能查看和修改少量"危险权限"（如相机、定位、麦克风）。但系统内部还有一类更细粒度的 **AppOps 应用操作权限**（例如：后台运行、读取通知、访问剪贴板、读取传感器、保持唤醒、读取应用使用情况等），这些权限对**隐私和电量**影响很大，普通用户却无法方便地查看和修改。

已 root 的技术用户希望有一个专门的工具，能够直观地查看和修改这些 AppOps 权限，并了解应用实际使用了哪些权限。

## 二、解决方案

开发一个 Android 应用「OPS 权限管家」，通过 **root 权限**调用系统 AppOps 能力，让用户能够：

1. **查看**手机上所有应用及其 AppOps 权限状态（允许 / 拒绝 / 询问 / 默认）。
2. **修改**单个应用的单个权限状态。
3. **批量管理**：一次对多个应用执行相同的权限操作（如批量拒绝某应用的"后台运行"）。
4. **审计**：查看应用使用权限的历史记录（系统已记录的），并预留实时监控扩展点。

## 三、用户故事

1. 作为已 root 的技术用户，我想要查看所有应用及其 AppOps 权限状态，以便了解哪些应用在后台运行或读取我的数据。
2. 作为已 root 的技术用户，我想要修改某个应用的某个权限（允许 / 拒绝 / 询问），以便控制该应用的行为。
3. 作为已 root 的技术用户，我想要批量对多个应用执行相同的权限操作，以便快速配置一批应用。
4. 作为已 root 的技术用户，我想要查看权限使用历史记录，以便审计哪些应用在何时使用了哪些权限。
5. 作为已 root 的技术用户，我想要在未 root 的设备上看到清晰的引导提示，以便知道如何为设备获取 root 后再使用本应用。

## 四、实现决策

### 4.1 技术栈
- 语言：Kotlin
- UI：Jetpack Compose（Material 3）
- 架构：MVVM + Repository 模式
- 依赖注入：Hilt（可选，若项目规模允许）
- 异步：Kotlin Coroutines + Flow

### 4.2 核心机制（root 方案）
- **修改权限**：通过 root 执行系统命令 `cmd appops set <包名> <操作> <模式>`，模式支持 `allow / deny / ignore / default` 等。
- **读取权限状态**：通过 root 执行 `cmd appops get <包名>`，解析输出得到该应用所有 AppOps 权限的状态。
- **历史记录**：通过 `dumpsys appops` 解析系统已记录的权限使用历史（含时间、次数），预留实时监控扩展点。
- 选择命令行方案而非反射调用隐藏 API，原因是**兼容性更好、跨 Android 10+ 版本更稳定**。

### 4.3 模块划分
| 模块 | 职责 |
| --- | --- |
| `AppOpsRepository` | 封装"读取权限状态 / 修改权限 / 读取历史记录"的 root 命令执行与解析（核心 seam） |
| `RootShell` | 封装 su 命令执行，检测 root 环境 |
| `AppListRepository` | 通过 PackageManager 获取已安装应用列表与信息 |
| `批量管理` | 对一组应用循环执行权限修改，展示进度与结果 |
| `历史记录` | 解析并展示权限使用历史 |
| `UI 层` | Compose 页面：应用列表、应用详情、权限分类、批量操作、历史记录、root 引导 |

### 4.4 关键界面
- **首页**：已安装应用列表（含搜索、按权限状态筛选）
- **应用详情**：该应用所有 AppOps 权限清单，可逐项切换状态
- **批量管理**：选择多个应用 → 选择权限 → 选择目标状态 → 批量执行
- **历史记录**：按应用或按权限查看使用历史
- **Root 引导**：检测无 root 时给出说明与引导

### 4.5 技术澄清 / 风险
- `cmd appops` 输出格式在不同 Android 版本上可能有差异，需要做解析适配层。
- Android 13+ 引入了新的权限模型，部分 AppOps 行为有变化，需在真机上验证。
- root 命令执行需在后台线程进行，避免阻塞 UI。
- 批量操作涉及多条命令，需异步执行并支持取消。

## 五、测试决策

- **好测试的定义**：不依赖真实 root 设备即可验证逻辑正确性。
- **测试 seam**：以 `AppOpsRepository` 为界，将"命令执行"与"输出解析"分离；测试时注入模拟的命令输出。
- **单元测试**：命令输出解析（`cmd appops get` / `dumpsys appops` 输出 → 数据模型）、批量操作逻辑、状态映射。
- **UI 测试**：Compose UI 测试（mock 数据）。
- **真机验证清单**：root 真机上验证查看 / 修改 / 批量 / 历史记录全流程，覆盖 Android 10、11、12、13+。

## 六、非范围（本期不做）

- 实时权限监控（预留扩展点，后续版本再做）
- 企业 MDM / 设备管理功能
- 卸载、冻结、清理应用等超出权限管理范围的功能
- iOS 或其他平台版本

## 七、进一步备注

- 应用需要 root，首次启动检测 root 环境并给出引导。
- 修改系统权限有一定风险，界面需给出明确提示（如"修改可能导致应用异常"）。
- 应用名「OPS 权限管家」为暂定，可调整。

---

## 验收标准（草稿）

1. 在已 root 的 Android 10+ 设备上，能列出所有已安装应用。
2. 能查看每个应用的 AppOps 权限状态，并成功修改（允许 / 拒绝 / 询问）。
3. 能批量对多个应用执行同一权限操作，并显示进度与结果。
4. 能查看权限使用历史记录。
5. 无 root 设备上显示引导提示，不崩溃。
6. 权限修改在重启后仍然生效（由系统持久化）。
