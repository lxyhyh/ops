# Issue 拆分：OPS 权限管家（Android）

> 来源：PRD.md（v0.1）
> 日期：2026-08-17
> 拆分方式：垂直切片（每个切片贯穿 数据获取 → 逻辑 → 界面 → 测试）

---

## 依赖关系图

```
[0] 项目骨架 + Root 检测
   ├──→ [1] 应用列表 + 权限查看 ──→ [2] 修改单个权限 ──→ [3] 批量管理
   └──→ [4] 权限使用历史记录（可与 1-3 并行）
[1][2][3][4] ──→ [5] 真机验证 + 文档 + 打包收尾
```

---

## Issue 0：项目骨架 + Root/权限环境检测与引导

- **状态**：已完成（2026-08-18 v0.2：重构为多执行器架构，支持 Root（su）与 Shizuku（ADB）两种修改方式，自动选择可用方式；新增设置页含主题切换）
- **被谁阻塞**：无（最先执行）
- **覆盖用户故事**：故事 5
- **描述**：
  - 搭建 Android 项目（Kotlin + Jetpack Compose + Material 3 + MVVM 架构）
  - 建立命令执行抽象 `CommandExecutor` 与路由层 `CommandExecutorRouter`：封装 su 与 Shizuku 执行，检测权限环境
  - 建立 `AppOpsRepository` 接口与实现骨架（核心 seam，命令执行与输出解析分离）
  - 无可用权限方式时显示引导界面（说明如何获取 root / Shizuku），不崩溃
- **验收标准**：
  - 项目可在 Android Studio 中编译通过
  - 无权限设备上启动显示引导界面，不崩溃
  - 有 root 或 Shizuku 设备上能检测到并进入主界面

---

## Issue 1：应用列表 + 权限状态查看

- **状态**：已完成（2026-08-17 修复：识别权限过少——① 权限目录扩展至 AOSP 完整清单（155 项系统权限 + 7 个别名，共 162 项），覆盖 Android 10~16 全部系统权限，并修正 FINE_LOCATION/COARSE_LOCATION 等错误名称；② 解析器不再丢弃目录外的权限，系统返回的全部权限都会展示）
- **被谁阻塞**：Issue 0
- **覆盖用户故事**：故事 1
- **描述**：
  - 通过 PackageManager 获取已安装应用列表（含应用名、图标、包名）
  - 通过 root 执行 `cmd appops get <包名>` 并解析输出，得到该应用所有 AppOps 权限状态
  - 实现应用列表页（含搜索、按权限状态筛选）
  - 实现应用详情页（只读展示该应用全部权限状态，按权限组分类）
- **验收标准**：
  - 能列出所有已安装应用
  - 点击应用能看到其全部 AppOps 权限状态（允许 / 拒绝 / 询问 / 默认）
  - 解析逻辑有单元测试覆盖（mock 命令输出）

---

## Issue 2：修改单个应用权限

- **状态**：已完成（含批量复用 `setAppOp` seam）
- **被谁阻塞**：Issue 1
- **覆盖用户故事**：故事 2
- **描述**：
  - 通过 root 执行 `cmd appops set <包名> <操作> <模式>` 修改单个权限
  - 应用详情页支持切换权限状态（允许 / 拒绝 / 询问 / 默认）
  - 修改后刷新显示，失败时给出错误提示
  - 界面给出风险提示（"修改可能导致应用异常"）
- **验收标准**：
  - 能成功修改单个权限并即时反映到界面
  - 修改失败时提示错误原因
  - 批量操作逻辑（循环执行 + 进度）有单元测试覆盖

---

## Issue 3：批量管理权限

- **状态**：已完成（2026-08-17 修复：批量页选择权限时闪退——权限下拉列表嵌套在滚动列表内导致崩溃，改为普通 Column）
- **被谁阻塞**：Issue 2
- **覆盖用户故事**：故事 3
- **描述**：
  - 支持多选应用
  - 选择要修改的权限与目标状态
  - 批量执行（异步、支持取消），展示进度与每个应用的结果
- **验收标准**：
  - 能一次对多个应用执行同一权限操作
  - 显示执行进度与成功 / 失败结果
  - 批量执行中可取消

---

## Issue 4：权限使用历史记录

- **状态**：已完成
- **被谁阻塞**：仅 Issue 0（可与 Issue 1-3 并行开发）
- **覆盖用户故事**：故事 4
- **描述**：
  - 通过 `dumpsys appops` 解析系统已记录的权限使用历史（含时间、次数）
  - 实现历史记录页：按应用或按权限查看
  - 预留实时监控扩展点（接口设计上可扩展）
- **验收标准**：
  - 能查看应用在何时使用了哪些权限
  - 历史记录解析逻辑有单元测试覆盖（mock 输出）
  - 接口预留实时监控扩展

---

## Issue 5：真机验证 + 文档 + 打包收尾

- **状态**：进行中（打包已完成，真机验证待做）
- **被谁阻塞**：Issue 1-4
- **覆盖用户故事**：全部
- **描述**：
  - 在 root 真机上验证全流程（查看 / 修改 / 批量 / 历史记录）
  - 覆盖 Android 10、11、12、13+ 版本验证
  - 完善 README（使用说明、root 引导、风险提示）
  - 更新 PRD / 需求文档状态
  - 打包 APK，确认可安装
- **验收标准**：
  - 全流程在 root 真机验证通过
  - 文档完整，APK 可安装使用

---

## 备注

- 每个切片完成后应独立验证（编译通过 + 该切片验收标准满足）再进入下一个。
- 若开发过程中发现 `cmd appops` 输出格式在特定 Android 版本有差异，记录到 Issue 对应切片并补充解析适配。

---

## 深度优化 / 多技能审查发现存档

> 来源：2026-08-18 全量深度优化（vibe编码工作流 排序驱动：Issue分类 → 代码审查 → 代码设计 → 依赖治理 → 功能实现 → 性能优化 → 架构改进 → 测试策略 → 调试诊断 → 缺陷修复）

### 已修复项（随提交落地）

| 类别 | 发现 | 修复 |
|------|------|------|
| 安全 | `setAppOp` 包名未校验，存在命令注入风险 | `RealAppOpsRepository` 用 `PACKAGE_NAME_REGEX` 校验，非法抛 `InvalidPackage` |
| 健壮性 | `ProcessRunner` 串行读 stdout/stderr 可能死锁 | 并发消费两个流 + 30s 超时 `destroyForcibly()` |
| 并发 | ViewModel 吞掉 `CancellationException`，取消失效 | 统一 `catch (e: CancellationException) { throw e }`；批量循环加 `ensureActive()` |
| 解析 | Parser 时间戳小数位不定、非法行致崩溃 | 限定 3 位小数 + 行级容错；Parser 改为可注入 class |
| 架构 | app 层依赖具体 `CommandExecutorRouter`，耦合执行实现 | 抽出 `ExecutionAvailability` 接口，app 层只依赖可用性抽象；Shizuku 可用性以 `ShizukuManager` StateFlow 为单一真相 |
| 架构 | `AppOpsParser` 单例 + 日期格式紧耦合 | 构造注入，测试与实际穿透同一 seam |
| 性能 | 批量收集 `results` 时反复整体 copy，近似 O(n²) | 外部 `mutableList` 累积，仅更新进度 StateFlow |
| 一致性 | 主题色硬编码，与 Material3 配色脱节 | 由 `colorScheme` 派生，明暗自适应 |
| UI | Shizuku 反射异常裸崩 | 反射调用 try/catch，映射为 `CommandFailed` |

### 真机验证待办（尚未在 root/Shizuku 真机执行）

- Android 10 / 11 / 12 / 13+ 上 `cmd appops get` 与 `dumpsys appops` 输出格式差异适配是否齐全
- Shizuku 授权 → 生效 → 修改权限的端到端流程
- 批量操作长列表取消后的状态一致性
- 权限历史记录页在真实 `dumpsys appops` 输出上的解析正确性
- 设置页 Root / Shizuku / AUTO 三种模式切换的实际执行通道是否符合预期
