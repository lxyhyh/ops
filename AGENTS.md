# AGENTS.md — 项目协作规则

本文件定义在本仓库工作时必须遵守的规则。所有 Agent 与协作者一律遵循。

## 工作原则

1. **注意任务边界，不要擅自扩大任务范围。**
   只做被要求做的事；发现额外问题时记录下来汇报，不擅自顺手修改。

2. **进行任务时一小步一小步推进并提交，让项目能及时修改或回档和审查。**
   每次提交只包含一个独立、完整的改动；提交信息清晰描述改动内容与原因；改动粒度以"可独立审查、可随时回档"为准。

3. **以最小正常运行代码实现功能。**
   不做过度设计，不引入非必要依赖；改动保持最小、可编译、可运行。

4. **审查出问题需要进行核实。**
   审查或代码评审中发现的问题，先核实事实与影响（读代码、查文档、复现验证），确认后再处理或报告；不凭印象下结论。

5. **遇到问题及时沟通，不擅自决定。**
   遇到阻塞、歧义、或需要取舍的决策点时，先与用户沟通确认；不擅自替用户做方向性决定。

## 提交规范

- 一次提交 = 一个逻辑改动（fix / chore / docs / refactor 分开提交）。
- 提交信息格式：`<type>: <简要说明>`（type: fix | chore | docs | refactor | test）。
- 提交前确认改动可回档：保持工作区小步推进，避免把无关改动混入同一提交。
